<%= form_for(@evento) do |f| %>
  <% if @evento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@evento.errors.count, "errores") %> Este evento no pudo ser guardado:</h2>

      <ul>
      <% @evento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label "Descripción del evento" %><br>
    <%= f.text_area :descripcion %>
  </div>
  <div class="field">
    <%= f.label "Fecha del evento" %><br>
    <%= f.date_select :fecha %>
  </div>
  <div class="actions">
    <%= f.submit "Crear evento" %>
  </div>
<% end %>
